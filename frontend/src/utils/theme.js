// A mutable global object that GSAP can animate directly.
// This allows 60FPS read access in ParticleBackground.jsx without forcing DOM recalculations.
export const globalTheme = {
  hue: 270 // Default to Purple (Hero)
};
